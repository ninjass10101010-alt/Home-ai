# 2026-05-24 Daily Log

## Dashboard: Schedule sorting + auto-hide fix
- Jeff reported daily schedule showing items in random order, not chronological
- Fixed three bugs in Home-ai dashboard (commit `e5ccd86`):

**ScheduleDisplay.tsx:**
- Broken sort: old code stripped `:` from "8:00 AM" → "800 AM" then used string localeCompare, which sorts "800 AM" > "1000 AM" (because "8" > "1" in ASCII)
- Added `parseTimeToMinutes()` helper that converts "8:00 AM" → 480, "2:30 PM" → 870 for numeric comparison
- Past items now auto-hidden (filtered out) instead of just strikethrough
- Shows "All done for today 🎉" when all items are in the past

**db/index.ts:**
- `selectTodaysEvents()`: added `.sort()` on raw 24h time strings before formatting
- `selectTodaysSchedules()`: moved `.sort()` BEFORE `.map()` so it sorts raw "07:00" strings instead of formatted "7:00 AM" strings (which broke AM/PM ordering)

Dashboard rebuilt and restarted on port 3001, verified 200 OK.
